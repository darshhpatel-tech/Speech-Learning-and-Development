const deployUrl = process.env.NEXT_PUBLIC_VERCEL_URL 
  ? `https://${process.env.NEXT_PUBLIC_VERCEL_URL}` 
  : 'http://localhost:3000';


// Access the system-defined deployment URL
const deployUrl = process.env.DEPLOY_URL || 'http://localhost:8080';

console.log(`Server is running publicly at: ${deployUrl}`);
